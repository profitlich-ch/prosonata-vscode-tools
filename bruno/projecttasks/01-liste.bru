meta {
  name: Projektaufgaben auflisten
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/projecttasks?perPage=10
  body: none
  auth: none
}

params:query {
  perPage: 10
}

assert {
  res.status: eq 200
}

docs {
  `GET /api/v1/projecttasks`

  Antwortumschlag `{ meta, data }`; in `meta` stehen `perPage`, `page`, `totalCount`,
  `apiLimitRemaining` und `apiLimitReset`.

  Die ID für den Einzelabruf hier entnehmen und als `projectTaskId` in die `.env` eintragen.
}
